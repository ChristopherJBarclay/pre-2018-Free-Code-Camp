
function sumAll(arr) {
  var sum = 0;
  
  if (arr[0] <= arr[1]) {
    for (var a = arr[0]; a <= arr[1]; a++) {
      sum+=a;
    }
  } else {
    for (var b = arr[1]; b <= arr[0]; b++) {
      sum+=b;
    }
  }
  
  
  return sum;
}

sumAll([4, 1]);
