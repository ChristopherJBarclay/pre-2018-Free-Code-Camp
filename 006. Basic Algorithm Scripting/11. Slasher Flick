function slasher(arr, howMany) {
  // .slice startingt at index number 'howMany', end at arr.length 
  //    (because .slice does not include the char you end on)
  return arr.slice(howMany, arr.length);
}

//test: 
slasher([1, 2, 3], 2);
