function repeatStringNumTimes(str, num) {
  if (num<0) {
    return "";
  } else {
    var repeated = "";
    
    for (i=0;i<num;i++) {
      repeated+=str;
    }
    
    return repeated;
  }
}

repeatStringNumTimes("abc", 3);
