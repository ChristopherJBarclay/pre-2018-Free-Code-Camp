

<script>

  // This is the document ready function of jQuery, which only executes once the entire page has loaded (very useful!)
  $(document).ready(function() {
    
    // This is the click event handler, which displays a message (using id '#getMessage' ) when the button with 'message' class is clicked.
    $("#getMessage").on("click", function() {
      
     
    });
   
  });
  
</script>
