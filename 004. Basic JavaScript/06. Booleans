/* Boolean structure and usage:

      true and false, if and else statements, if/else chaining (order is important), 
      
      strict vs not strict operators for: 
            equality (== compares two values vs. === compares data types and values), 
            inequality (!= vs. !==), 
            > and < vs. >= and <=
      
      operators:
            && and
            || or
